--- a/site/index.html
+++ b/site/index.html
     <meta charset='utf-8'>
     <meta name='viewport' content='width=device-width, initial-scale=1'>
     <title>BVT Diff Generator</title>
-    <meta name='description' content='Realtime animation of Codex with and without the BVT diff proxy.'>
+    <meta name='description' content='Horizontal realtime comparison of standard Codex chat speed versus a causally equivalent BVT diff proxy trace.'>
     <style>
       :root {
         color-scheme: dark;
-        --bg: #0d1117;
-        --panel: #111821;
-        --panel-2: #0f161d;
-        --ink: #eef6ee;
-        --muted: #9aa8a0;
-        --line: #27313a;
-        --bad: #ff7a7a;
-        --good: #31d59b;
-        --blue: #77a7ff;
-        --gold: #f4c95d;
+        --bg: #080c10;
+        --panel: #10171f;
+        --panel2: #0c1319;
+        --ink: #eef7f0;
+        --muted: #9aa9a2;
+        --line: #25323d;
+        --green: #34d399;
+        --blue: #7aa7ff;
+        --red: #ff7c7c;
+        --amber: #f5c75d;
       }
       * { box-sizing: border-box; }
       body {
         margin: 0;
         min-height: 100vh;
-        background:
-          radial-gradient(circle at 18% 10%, rgba(49, 213, 155, .15), transparent 28%),
-          radial-gradient(circle at 84% 18%, rgba(119, 167, 255, .16), transparent 30%),
-          var(--bg);
+        background: linear-gradient(180deg, #0b1016 0%, #080c10 100%);
         color: var(--ink);
         font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
       }
-      main { padding: 34px clamp(16px, 4vw, 56px) 48px; }
-      .top { display: flex; justify-content: space-between; gap: 24px; align-items: start; margin-bottom: 28px; }
-      h1 { margin: 0; font-size: clamp(42px, 8vw, 92px); line-height: .9; letter-spacing: 0; max-width: 900px; }
-      .lead { max-width: 760px; color: var(--muted); font-size: 20px; margin: 18px 0 0; }
+      main { padding: 28px clamp(18px, 3vw, 42px) 36px; }
+      .top { display: grid; grid-template-columns: minmax(0, 1fr) auto; gap: 24px; align-items: start; margin-bottom: 20px; }
+      h1 { margin: 0; font-size: clamp(42px, 7vw, 86px); line-height: .9; letter-spacing: 0; }
+      .lead { max-width: 940px; margin: 14px 0 0; color: var(--muted); font-size: 19px; }
       .links { display: flex; gap: 10px; flex-wrap: wrap; justify-content: end; }
-      .button { color: var(--ink); text-decoration: none; border: 1px solid var(--line); padding: 10px 13px; background: rgba(17, 24, 33, .76); font-weight: 700; }
-      .button.primary { background: var(--good); border-color: var(--good); color: #07110d; }
-      .stage { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; min-height: 560px; }
-      .lane { background: rgba(17, 24, 33, .88); border: 1px solid var(--line); overflow: hidden; display: grid; grid-template-rows: auto auto 1fr auto; }
-      .lane.proxy { border-color: rgba(49, 213, 155, .55); box-shadow: 0 0 0 1px rgba(49, 213, 155, .12) inset; }
-      .lane-head { padding: 16px 18px; display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid var(--line); }
-      .lane h2 { margin: 0; font-size: 20px; letter-spacing: 0; }
-      .pill { font: 700 12px ui-monospace, SFMono-Regular, Menlo, monospace; color: var(--muted); border: 1px solid var(--line); padding: 5px 8px; }
-      .proxy .pill { color: var(--good); border-color: rgba(49, 213, 155, .55); }
-      .meters { display: grid; grid-template-columns: repeat(3, 1fr); border-bottom: 1px solid var(--line); }
-      .meter { padding: 14px 16px; border-right: 1px solid var(--line); min-width: 0; }
+      .button { color: var(--ink); text-decoration: none; border: 1px solid var(--line); padding: 10px 13px; background: rgba(16, 23, 31, .84); font-weight: 750; }
+      .button.primary { color: #06100c; background: var(--green); border-color: var(--green); }
+      .stage-wrap { overflow-x: auto; padding-bottom: 8px; }
+      .stage { min-width: 1120px; display: grid; grid-template-columns: 1fr 1fr; gap: 14px; align-items: stretch; }
+      .lane { min-height: 680px; background: rgba(16, 23, 31, .94); border: 1px solid var(--line); display: grid; grid-template-rows: auto auto 1fr auto; overflow: hidden; }
+      .lane.fast { border-color: rgba(52, 211, 153, .6); box-shadow: inset 0 0 0 1px rgba(52, 211, 153, .18); }
+      .lane-head { padding: 14px 16px; border-bottom: 1px solid var(--line); display: flex; align-items: center; justify-content: space-between; gap: 12px; }
+      h2 { margin: 0; font-size: 19px; letter-spacing: 0; }
+      .tag { border: 1px solid var(--line); padding: 5px 8px; color: var(--muted); font: 700 12px ui-monospace, SFMono-Regular, Menlo, monospace; white-space: nowrap; }
+      .fast .tag { color: var(--green); border-color: rgba(52, 211, 153, .55); }
+      .meters { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); border-bottom: 1px solid var(--line); }
+      .meter { min-width: 0; padding: 12px 14px; border-right: 1px solid var(--line); }
       .meter:last-child { border-right: 0; }
-      .meter span { display: block; color: var(--muted); font-size: 12px; }
-      .meter strong { display: block; margin-top: 4px; font: 800 24px ui-monospace, SFMono-Regular, Menlo, monospace; white-space: nowrap; }
-      .terminal { position: relative; padding: 16px; overflow: hidden; background: linear-gradient(180deg, rgba(15, 22, 29, .94), rgba(10, 15, 20, .98)); }
-      .terminal pre { margin: 0; min-height: 360px; white-space: pre-wrap; font: 13px/1.55 ui-monospace, SFMono-Regular, Menlo, monospace; color: #d9e6da; }
-      .terminal::after { content: ''; position: absolute; inset: auto 0 0; height: 70px; background: linear-gradient(transparent, rgba(10, 15, 20, .97)); pointer-events: none; }
-      .diff { display: grid; grid-template-columns: 72px 1fr; gap: 0; border-top: 1px solid var(--line); font: 12px/1.45 ui-monospace, SFMono-Regular, Menlo, monospace; }
-      .diff div { padding: 5px 8px; border-bottom: 1px solid rgba(39, 49, 58, .65); }
-      .minus { color: var(--bad); }
-      .plus { color: var(--good); }
-      .line-no { color: #607080; background: rgba(255, 255, 255, .025); text-align: right; }
-      .bars { margin-top: 18px; display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
-      .bar-wrap { border: 1px solid var(--line); padding: 13px; background: rgba(17, 24, 33, .68); }
-      .bar-label { display: flex; justify-content: space-between; color: var(--muted); font-size: 13px; margin-bottom: 8px; }
-      .bar { height: 11px; background: #1b242d; overflow: hidden; }
-      .fill { height: 100%; width: 0%; background: var(--bad); transition: width .35s ease; }
-      .proxy-fill { background: linear-gradient(90deg, var(--good), var(--blue)); }
-      .receipt { margin-top: 16px; border: 1px solid rgba(49, 213, 155, .42); padding: 14px; color: var(--muted); font: 13px/1.45 ui-monospace, SFMono-Regular, Menlo, monospace; background: rgba(49, 213, 155, .06); }
-      .blink { display: inline-block; width: 8px; height: 15px; margin-left: 3px; background: var(--good); animation: blink 1s steps(2, start) infinite; transform: translateY(2px); }
+      .meter span { display: block; color: var(--muted); font-size: 11px; }
+      .meter strong { display: block; margin-top: 4px; font: 800 20px ui-monospace, SFMono-Regular, Menlo, monospace; white-space: nowrap; }
+      .workspace { display: grid; grid-template-columns: 1fr 170px; min-height: 0; }
+      .chat { padding: 14px; border-right: 1px solid var(--line); overflow: hidden; background: linear-gradient(180deg, rgba(12, 19, 25, .96), rgba(7, 12, 16, .98)); }
+      .messages { display: flex; flex-direction: column; gap: 9px; min-height: 480px; }
+      .msg { opacity: 0; transform: translateY(10px); animation: rise .24s ease forwards; max-width: 92%; padding: 9px 10px; border: 1px solid var(--line); background: rgba(255, 255, 255, .035); font-size: 13px; line-height: 1.35; }
+      .msg.user { align-self: flex-start; color: #dbeafe; border-color: rgba(122, 167, 255, .4); }
+      .msg.agent { align-self: flex-end; color: #dcfce7; border-color: rgba(52, 211, 153, .35); }
+      .msg.tool { align-self: stretch; max-width: 100%; color: #ffe9aa; border-color: rgba(245, 199, 93, .42); font-family: ui-monospace, SFMono-Regular, Menlo, monospace; }
+      .toolrail { padding: 14px 12px; background: rgba(255, 255, 255, .02); }
+      .toolrail h3 { margin: 0 0 12px; font-size: 12px; color: var(--muted); text-transform: uppercase; letter-spacing: .08em; }
+      .tool { margin-bottom: 12px; }
+      .tool-label { display: flex; justify-content: space-between; gap: 8px; color: var(--muted); font: 11px ui-monospace, SFMono-Regular, Menlo, monospace; margin-bottom: 5px; }
+      .bar { height: 8px; background: #17212a; overflow: hidden; border: 1px solid #21303a; }
+      .fill { height: 100%; width: 0%; background: var(--red); transition: width .18s linear; }
+      .fast .fill { background: linear-gradient(90deg, var(--green), var(--blue)); }
+      .diff { border-top: 1px solid var(--line); display: grid; grid-template-columns: 68px 1fr; font: 12px/1.42 ui-monospace, SFMono-Regular, Menlo, monospace; }
+      .diff div { padding: 5px 8px; border-bottom: 1px solid rgba(37, 50, 61, .65); }
+      .line-no { color: #657786; background: rgba(255, 255, 255, .025); text-align: right; }
+      .plus { color: var(--green); }
+      .minus { color: var(--red); }
+      .claim { margin-top: 14px; border: 1px solid rgba(122, 167, 255, .34); background: rgba(122, 167, 255, .06); padding: 12px 14px; color: var(--muted); font: 13px/1.45 ui-monospace, SFMono-Regular, Menlo, monospace; }
+      .ticker { display: inline-block; width: 8px; height: 14px; background: var(--green); margin-left: 4px; transform: translateY(2px); animation: blink 1s steps(2, start) infinite; }
+      @keyframes rise { to { opacity: 1; transform: translateY(0); } }
       @keyframes blink { 50% { opacity: 0; } }
-      @media (max-width: 980px) { .top, .stage, .bars { grid-template-columns: 1fr; display: grid; } .links { justify-content: start; } }
+      @media (max-width: 760px) { main { padding-inline: 14px; } .top { grid-template-columns: 1fr; } .links { justify-content: start; } }
     </style>
   </head>
   <body>
     <main>
-      <div class='top'>
+      <header class='top'>
         <div>
           <h1>BVT Diff Generator</h1>
-          <p class='lead'>The speedup is now measured as actual unified patch artifacts. Codex without the proxy hand-writes fragile diffs; Codex with the BVT proxy converts typed ops into about 44k candidate patches/sec on the local release benchmark.</p>
+          <p class='lead'>Same causal chat trace, two render speeds. The left lane plays like a standard Codex chat with sequential tool calls. The right lane keeps the causal order but lowers the trace into typed ops, so the visible unit is actual patch artifacts and tool-call-equivalent batches.</p>
         </div>
         <nav class='links'>
           <a class='button primary' href='https://github.com/j-94/bvt-diff-generator'>GitHub</a>
           <a class='button' href='.'>Live site</a>
         </nav>
-      </div>
+      </header>
 
-      <section class='stage' aria-label='Realtime Codex comparison'>
-        <article class='lane'>
-          <div class='lane-head'><h2>Codex without proxy</h2><span class='pill'>raw patch text</span></div>
-          <div class='meters'>
-            <div class='meter'><span>patches/sec</span><strong id='rawOps'>0</strong></div>
-            <div class='meter'><span>apply risk</span><strong id='rawRisk'>high</strong></div>
-            <div class='meter'><span>receipt</span><strong id='rawReceipt'>none</strong></div>
-          </div>
-          <div class='terminal'><pre id='rawStream'></pre></div>
-          <div class='diff' id='rawDiff'></div>
-        </article>
+      <div class='stage-wrap'>
+        <section class='stage' aria-label='Horizontal causal chat comparison'>
+          <article class='lane slow'>
+            <div class='lane-head'><h2>Standard Codex Chat</h2><span class='tag'>sequential tools</span></div>
+            <div class='meters'>
+              <div class='meter'><span>chat render</span><strong id='slowMsgRate'>0.8/s</strong></div>
+              <div class='meter'><span>tool calls</span><strong id='slowTools'>0</strong></div>
+              <div class='meter'><span>patches/sec</span><strong id='slowPatches'>manual</strong></div>
+              <div class='meter'><span>causal trace</span><strong id='slowTrace'>0%</strong></div>
+            </div>
+            <div class='workspace'>
+              <div class='chat'><div class='messages' id='slowMessages'></div></div>
+              <aside class='toolrail'><h3>Tool Calls</h3><div id='slowToolsList'></div></aside>
+            </div>
+            <div class='diff' id='slowDiff'></div>
+          </article>
 
-        <article class='lane proxy'>
-          <div class='lane-head'><h2>Codex with BVT proxy</h2><span class='pill'>ICL -> DSL -> ops</span></div>
-          <div class='meters'>
-            <div class='meter'><span>patches/sec</span><strong id='proxyOps'>0</strong></div>
-            <div class='meter'><span>apply risk</span><strong id='proxyRisk'>gated</strong></div>
-            <div class='meter'><span>patch bytes/sec</span><strong id='proxyReceipt'>34MB</strong></div>
-          </div>
-          <div class='terminal'><pre id='proxyStream'></pre></div>
-          <div class='diff' id='proxyDiff'></div>
-        </article>
-      </section>
-
-      <section class='bars'>
-        <div class='bar-wrap'>
-          <div class='bar-label'><span>Raw patch drift</span><strong id='rawPct'>manual</strong></div>
-          <div class='bar'><div class='fill' id='rawBar'></div></div>
-        </div>
-        <div class='bar-wrap'>
-          <div class='bar-label'><span>Ops converted into patch artifacts</span><strong id='proxyPct'>0%</strong></div>
-          <div class='bar'><div class='fill proxy-fill' id='proxyBar'></div></div>
-        </div>
-      </section>
+          <article class='lane fast'>
+            <div class='lane-head'><h2>Codex + BVT Diff Proxy</h2><span class='tag'>same trace, lowered</span></div>
+            <div class='meters'>
+              <div class='meter'><span>chat render</span><strong id='fastMsgRate'>trace burst</strong></div>
+              <div class='meter'><span>tool-call equiv</span><strong id='fastTools'>0/s</strong></div>
+              <div class='meter'><span>patches/sec</span><strong id='fastPatches'>0</strong></div>
+              <div class='meter'><span>causal trace</span><strong id='fastTrace'>0%</strong></div>
+            </div>
+            <div class='workspace'>
+              <div class='chat'><div class='messages' id='fastMessages'></div></div>
+              <aside class='toolrail'><h3>Tool Batches</h3><div id='fastToolsList'></div></aside>
+            </div>
+            <div class='diff' id='fastDiff'></div>
+          </article>
+        </section>
+      </div>
 
-      <div class='receipt' id='receiptLine'>Benchmark: 1.412M ops/sec converts to 44,122 actual unified patches/sec at 776 bytes/patch<span class='blink'></span></div>
+      <div class='claim'>Measured local release bench: 320,000 typed ops -> 10,000 unified patch artifacts in ~0.23s, about 41k-44k patches/sec. Tool-call equivalent is theoretical: it assumes the same causal trace is represented as local typed patch batches, not networked shell/API calls.<span class='ticker'></span></div>
     </main>
 
     <script>
-      const rawLines = [
-        'read file, infer line numbers, hand-write one patch...',
-        'maybe replace beta, maybe insert delta, re-count context...',
-        'emit patch text; hope hunk headers match current file...',
-        'retry after context drift; no typed receipt yet...',
-        'patch applies? maybe. semantic boundary still unclear.'
+      const trace = [
+        { role: 'user', text: 'Make the site show the actual speedup, not just ops/sec.' },
+        { role: 'agent', text: 'Plan: preserve causal order, compare standard chat against BVT proxy.' },
+        { role: 'tool', text: 'hotgraph(query) -> weak prior, use local receipt evidence' },
+        { role: 'agent', text: 'Compile high-level DSL into typed edit operations.' },
+        { role: 'tool', text: 'compile-dsl -> plan.json' },
+        { role: 'tool', text: 'generate -> unified.diff + receipt.json' },
+        { role: 'tool', text: 'apply -> site/index.html' },
+        { role: 'agent', text: 'Result: same trace, visible patches/sec and tool batch speed.' }
       ];
-      const proxyLines = [
-        'ICL: examples select the transition shape',
-        'DSL: replace_line_containing(example.txt, beta, BETA)',
-        'DSL: insert_after_containing(example.txt, gamma, delta)',
-        'compile-dsl -> typed ops; bench converts 320k ops into 10k patches',
-        'generate -> 10,000 unified patch artifacts in 227ms',
-        'apply -> S prime only after control checks'
-      ];
-      const rawDiff = [['-', 'beta'], ['+', 'BETA?'], ['?', 'hunk context guessed']];
-      const proxyDiff = [[' ', 'alpha'], ['-', 'beta'], ['+', 'BETA'], [' ', 'gamma'], ['+', 'delta']];
-      const els = {
-        rawStream: document.getElementById('rawStream'),
-        proxyStream: document.getElementById('proxyStream'),
-        rawDiff: document.getElementById('rawDiff'),
-        proxyDiff: document.getElementById('proxyDiff'),
-        rawOps: document.getElementById('rawOps'),
-        proxyOps: document.getElementById('proxyOps'),
-        rawBar: document.getElementById('rawBar'),
-        proxyBar: document.getElementById('proxyBar'),
-        rawPct: document.getElementById('rawPct'),
-        proxyPct: document.getElementById('proxyPct'),
-        receiptLine: document.getElementById('receiptLine')
-      };
+      const tools = ['hotgraph', 'compile-dsl', 'generate-diff', 'apply', 'bench', 'deploy'];
+      const patchRows = [[' ', 'S: chat trace'], ['+', 'Delta: typed DSL actions'], ['+', 'C: line anchors + receipts'], ['+', 'S prime: unified patch artifacts'], ['+', 'tool-call batches: local typed ops']];
+      const slowMessages = document.getElementById('slowMessages');
+      const fastMessages = document.getElementById('fastMessages');
+      const slowToolsList = document.getElementById('slowToolsList');
+      const fastToolsList = document.getElementById('fastToolsList');
+      const slowDiff = document.getElementById('slowDiff');
+      const fastDiff = document.getElementById('fastDiff');
+      const slowTools = document.getElementById('slowTools');
+      const fastTools = document.getElementById('fastTools');
+      const fastPatches = document.getElementById('fastPatches');
+      const slowTrace = document.getElementById('slowTrace');
+      const fastTrace = document.getElementById('fastTrace');
       let tick = 0;
-      function append(pre, lines, label) {
-        const next = lines[tick % lines.length];
-        pre.textContent += label + ' ' + next + '\n';
-        const rows = pre.textContent.split('\n').slice(-10);
-        pre.textContent = rows.join('\n');
+
+      function msgNode(item) {
+        const el = document.createElement('div');
+        el.className = 'msg ' + item.role;
+        el.textContent = item.role + '> ' + item.text;
+        return el;
       }
-      function renderDiff(target, rows) {
+      function toolNode(name, pct, fast) {
+        const wrap = document.createElement('div');
+        wrap.className = 'tool';
+        wrap.innerHTML = '<div class="tool-label"><span>' + name + '</span><span>' + pct + '%</span></div><div class="bar"><div class="fill" style="width:' + pct + '%"></div></div>';
+        return wrap;
+      }
+      function renderDiff(target, count) {
         target.innerHTML = '';
-        rows.forEach((row, i) => {
-          const num = document.createElement('div');
-          num.className = 'line-no';
-          num.textContent = String(i + 1).padStart(2, '0');
+        patchRows.slice(0, count).forEach((row, idx) => {
+          const n = document.createElement('div');
+          n.className = 'line-no';
+          n.textContent = String(idx + 1).padStart(2, '0');
           const body = document.createElement('div');
           body.className = row[0] === '+' ? 'plus' : row[0] === '-' ? 'minus' : '';
           body.textContent = row[0] + ' ' + row[1];
-          target.append(num, body);
+          target.append(n, body);
         });
       }
-      function loop() {
-        tick += 1;
-        append(els.rawStream, rawLines, 'codex>');
-        append(els.proxyStream, proxyLines, 'proxy>');
-        const raw = Math.min(94, 18 + tick * 7);
-        const proxy = Math.min(100, 26 + tick * 13);
-        els.rawOps.textContent = String(20 + (tick % 5) * 7);
-        els.proxyOps.textContent = tick < 8 ? String((tick * 5500).toLocaleString()) : '44k';
-        els.rawBar.style.width = raw + '%';
-        els.proxyBar.style.width = proxy + '%';
-        els.rawPct.textContent = raw + '%';
-        els.proxyPct.textContent = proxy + '%';
-        renderDiff(els.rawDiff, rawDiff.slice(0, 1 + (tick % rawDiff.length)));
-        renderDiff(els.proxyDiff, proxyDiff.slice(0, 1 + (tick % proxyDiff.length)));
-        els.receiptLine.innerHTML = tick % 3 === 0
-          ? 'Receipt: 320k ops -> 10k patch artifacts -> 7.76MB diff output in 227ms<span class=\'blink\'></span>'
-          : 'S: repo files | Delta: typed ops | C: render/apply checks | S\': actual unified patches<span class=\'blink\'></span>';
+      function render() {
+        tick = (tick + 1) % 18;
+        const slowCount = Math.min(trace.length, Math.floor(tick / 2) + 1);
+        const fastCount = Math.min(trace.length, tick < 2 ? tick + 2 : trace.length);
+        slowMessages.innerHTML = '';
+        fastMessages.innerHTML = '';
+        trace.slice(0, slowCount).forEach(item => slowMessages.appendChild(msgNode(item)));
+        trace.slice(0, fastCount).forEach(item => fastMessages.appendChild(msgNode(item)));
+        slowToolsList.innerHTML = '';
+        fastToolsList.innerHTML = '';
+        const slowToolCount = Math.max(0, slowCount - 2);
+        const fastToolCount = Math.min(tools.length, tick + 1);
+        tools.forEach((tool, idx) => {
+          slowToolsList.appendChild(toolNode(tool, idx < slowToolCount ? 100 : idx === slowToolCount ? 45 : 0));
+          fastToolsList.appendChild(toolNode(tool, idx < fastToolCount ? 100 : 0));
+        });
+        const slowPct = Math.round((slowCount / trace.length) * 100);
+        const fastPct = Math.round((fastCount / trace.length) * 100);
+        slowTools.textContent = String(Math.min(slowToolCount, tools.length));
+        fastTools.textContent = tick < 8 ? (tick * 520).toLocaleString() + '/s' : '4.1k/s';
+        fastPatches.textContent = tick < 8 ? (tick * 5500).toLocaleString() : '44k';
+        slowTrace.textContent = slowPct + '%';
+        fastTrace.textContent = fastPct + '%';
+        renderDiff(slowDiff, Math.min(2, Math.max(1, slowToolCount)));
+        renderDiff(fastDiff, Math.min(patchRows.length, fastToolCount));
       }
-      renderDiff(els.rawDiff, rawDiff.slice(0, 1));
-      renderDiff(els.proxyDiff, proxyDiff.slice(0, 1));
-      loop();
-      setInterval(loop, 900);
+      render();
+      setInterval(render, 850);
     </script>
   </body>
 </html>
